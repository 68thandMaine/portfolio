describe('sidebar navigation behavior', () => {
  context('Menu Icon', () => {
    it('the menu button is fixed on the page and is visible', () => {

    });
    it('clicking the menu icon opens the sidebar menu and hides the menu button', () => {

    });
  })
})